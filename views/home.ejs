<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Featured Albania: Europe’s Hidden Gem</title>
  <link rel="stylesheet" href="/css/home.css">
</head>
<body>

  <!-- Hero with fixed height -->
  <div
    class="hero bg-cover bg-center h-[400px] relative"
    style="background-image: url('/images/ksamil.jpg')"
  >
    <div class="hero-overlay bg-black/50 absolute inset-0"></div>
    <div class="hero-content text-neutral-content text-center absolute inset-0 flex flex-col justify-center">
      <div class="max-w-lg mx-auto">
        <h1 class="mb-3 text-4xl md:text-5xl font-bold">Featured Albania: Europe’s Hidden Gem</h1>
        <p class="mb-5 text-lg md:text-xl">Discover Europe’s hidden charms—from historic cities to breathtaking landscapes.</p>
        <a href="/memories/add" class="btn btn-primary">Explore Europe</a>
      </div>
    </div>
  </div>

  <div class="max-w-6xl mx-auto my-10 px-4">
    <div class="flex justify-end mb-4">
      <a href="/memories/add" class="btn btn-active btn-primary">Add Location</a>
    </div>

    <div class="overflow-x-auto">
      <table class="table w-full">
        <thead>
          <tr class="bg-neutral-content">
            <th></th>
            <th>Name</th>
            <th>Category</th>
            <th>Address</th>
            <th>Comments</th>
            <th>Action</th>
          </tr>
        </thead>
        <tbody>
          <% locations.forEach(location => { %>
            <tr>
              <td>
                <img src="<%= location.image %>" class="w-48 rounded-lg" />
              </td>
              <td>
                <span class="font-bold"><%= location.name %></span><br />
                <span class="badge badge-primary badge-sm badge-outline"><%= location.category %></span>
              </td>
              <td><%= location.category %></td>
              <td><%= location.address %></td>
              <td><%= location.comments %></td>
              <td>
                <a href="/memories/delete/<%= location.id %>" class="btn btn-default btn-xs">Delete</a>
              </td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
  </div>

</body>
</html>
